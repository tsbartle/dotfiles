# tmux automation for dev setup

selectp -t 1 # select the first (0) pane

splitw -v -p 25 # split it into two HORIZONTAL with a height of XX

selectp -t 2 # select the new, second (1) pane
splitw -h -p 50 # split it into two halves VERTICAL
selectp -t 1 # go back to the first pane
